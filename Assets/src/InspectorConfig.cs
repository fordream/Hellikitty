using UnityEngine;
using System.Collections;

public class InspectorConfig : MonoBehaviour {

    public static float grid_point_seperation = 1.0f;          //the seperation value between each point in the grid
    public static float grid_collider_offset = 1.0f;           //the larger the collider offset, the larger objects will be when calculating grid points
}
